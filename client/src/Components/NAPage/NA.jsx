import React from "react";

const NA = ({ message }) => {
  return (
    <div className="flex justify-center mt-64 font-semibold text-2xl">
      <h1>{message}</h1>
    </div>
  );
};

export default NA;
